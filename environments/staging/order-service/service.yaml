apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: staging
  labels:
    app: order-service
    app.kubernetes.io/part-of: company-platform

spec:
  type: ClusterIP

  selector:
    app: order-service

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
