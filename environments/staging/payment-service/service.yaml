apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: staging
  labels:
    app: payment-service
    app.kubernetes.io/part-of: company-platform

spec:
  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
